// Helper function
function multiplyByTwo(number) {
    return number * 2;
  }
  
  // Main code
  let inputNumber = 5;
  let result = multiplyByTwo(inputNumber);
  
  console.log(`The result of multiplying ${inputNumber} by 2 is: ${result}`);
  